# 安全加固要求

## 1. 认证与授权
- 强制启用 HTTPS。
- 密码必须加盐哈希存储 (BCrypt/Argon2)。
- 敏感接口必须进行 RBAC 权限校验。

## 2. 数据安全
- 敏感数据（如身份证、手机号）加密存储。
- 严禁在日志中打印敏感数据。

## 3. 防护策略
- **SQL 注入**: 必须使用参数化查询或 ORM。
- **XSS**: 必须对用户输入进行转义。
- **CSRF**: 启用 CSRF Token 验证。
